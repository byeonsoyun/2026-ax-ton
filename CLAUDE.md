# CLAUDE.md

이 파일은 Claude Code 가 세션마다 자동으로 읽습니다.
**아무것도 붙여넣지 않아도 아래 규칙이 적용됩니다.**

---

## ★ 2026-08-21 — 개인 개발로 전환됨

**이 프로젝트는 변소윤 한 사람이 진행합니다.** 팀 개발로 시작했지만 지금은 혼자입니다.

### 그래서 건너뛰는 것

- **포지션(P1~P4) 확인 절차를 하지 않습니다.** 물어보지도 마세요.
  브랜치는 `feature/byeonsoyun`, 담당은 `src/` 전체입니다
- **소유권 규칙이 해당 없습니다.** "1화면 = 주인 1명" · "남의 파일 고치기 전에 물어보기"
  · "공용 파일은 팀에 공유" 는 네 명이 동시에 만들 때의 규칙이었습니다.
  어느 파일이든 바로 고치면 됩니다
- **PR 을 만들지 않습니다.** 리뷰할 사람이 없습니다. `feature/byeonsoyun` 에 push 하고,
  덩어리가 끝나면 `develop` 에 머지합니다

### 그래서 그대로 지키는 것

아래는 혼자여도 유효합니다. **오히려 혼자면 어기기 쉬워지니 더 봐야 합니다.**

- **코드 규칙 전부** — 외부 요청 0건 · `innerHTML` 금지 · ES 모듈 금지 · 터치 타깃 60px
  · 색상만으로 상태 구분 금지
- **데이터 계약** — `localStorage` 를 직접 부르지 않고 `Store` 를 거칩니다.
  8개 키의 모양을 바꿀 때는 그 키를 읽는 화면을 전부 확인합니다
- **제품 원칙** — 검수된 문구만 안전 지시로 · 신고는 익명 · 미이수를 숨기지 않음
  · "면책" 표현 금지 · 미통과는 교육의 실패
- **git 규칙** — `main` 직접 push 금지 · `--force` 와 `reset --hard` 금지 · 훅 건너뛰기 금지
- **개발일지** — 덩어리가 끝날 때마다 `docs/devlog/byeonsoyun.md` 에 한 줄.
  발표 대본의 재료가 여기서 나옵니다

### 세션을 시작하면

1. `git branch --show-current` — `feature/byeonsoyun` 인지 확인.
   `main` 이나 `develop` 이면 작업하지 말고 옮기세요
2. `docs/07-next-tasks.md` 를 읽습니다 — **하나의 우선순위 목록**입니다.
   맨 위 **"★ 지난 세션이 끝난 자리"** 에 지금 상태와 다음 한 걸음이 적혀 있습니다.
   **그것만으로 이어갈 수 있습니다**
3. 더 자세한 배경이 필요하면 `docs/devlog/byeonsoyun.md` 맨 위 항목을 봅니다
   (특히 "막혔던 것 / 어떻게 풀었는지")
4. **지금 상태를 한 문단으로 말하고, `- [ ]` 중 맨 위 것 하나를 제안합니다.**
   여러 개를 동시에 벌이지 않습니다
5. 한 항목이 끝나면 `docs/07-next-tasks.md` 의 `- [ ]` 를 `- [x]` 로 바꾸고,
   **"★ 지난 세션이 끝난 자리" 표를 갱신하고**, 개발일지와 함께 커밋합니다

> **"나 뭐 하면 되는지 알려줘"** 나 **"docs 보고 바로 진행해줘"** 라고만 오면
> 되묻지 말고 위 1~4 를 그대로 하세요.

### 아래 문서는 팀 시절 기록입니다

`docs/02-positions.md` (포지션 카드 4장) · `docs/03-prompts.md` (포지션별 복붙 프롬프트)
는 **팀으로 진행하던 때의 기록**입니다. 지금은 `docs/07-next-tasks.md` 하나만 보면 됩니다.

다만 그 문서들의 **화면별 요구사항과 제약**은 여전히 정확합니다. 기획에서 온 것이라
포지션 구분과 무관하게 유효합니다.

---

## 프로젝트

**이주노동자 안전교육 이해도 검증 플랫폼.**
서명은 참석의 증거일 뿐 이해의 증거가 아니라는 문제에서 출발했습니다.
노동자가 글자를 읽지 않고도 이해했는지 확인하고, 그 기록을 그대로 법정 교육 증빙으로 만듭니다.

- 코드는 전부 `src/` 아래에 있습니다
- **정적 HTML + 바닐라 JS. 빌드가 없습니다.** `npm install` 도 `node_modules` 도 없습니다
- 확인은 `src/index.html` 을 브라우저로 여는 것으로 끝납니다
- 서버도 DB 도 없습니다. 모든 데이터는 브라우저 `localStorage` 에만 있습니다

### 배포는 Vercel — 살아 있습니다 (2026-08-25 첫 배포)

> **https://2026-ax-ton.vercel.app**

- **`main` 이 운영 브랜치입니다. `main` 에 올라가면 자동으로 배포됩니다**
- **Root Directory 는 `src`** 입니다. 그래야 `docs/` `tests/` 가 배포에 안 올라갑니다.
  확인했습니다 — `/docs/...` `/tests/...` `/CLAUDE.md` 는 전부 404 입니다
- 빌드 명령이 없습니다. 정적 파일을 그대로 서빙합니다. `vercel.json` 도 없습니다
- **`file://` 로도 계속 열리게 유지합니다.** 더블클릭 확인이 이 프로젝트에서 가장
  빠른 검증 방법이고, **ES 모듈 금지** 규칙이 그것을 지킵니다.
  Vercel 에서 된다고 그 규칙을 풀지 마세요 — 확인 방법 전체를 잃습니다
- **오리진별로 데이터가 갈립니다.** 배포 주소 · `localhost` · `file://` 의
  `localStorage` 가 전부 별개입니다. 배포 주소에 로컬 데이터가 안 보이는 것은 정상입니다
- 기기 사이로 데이터를 옮겨야 하면 기능1 화면의 **내보내기 / 불러오기**
  (`Store.exportAll()` `Store.importAll()`)를 씁니다

**프로젝트 전체 설명은 `docs/08-overview.md` 하나에 정리돼 있습니다** —
무엇을 만들었고, 무엇이 되고 안 되고, 데이터와 배포가 어떻게 돌아가고,
서버를 붙이면 어떻게 되는지까지. **누가 "이 프로젝트 뭐야" 라고 물으면 이 문서를 주세요.**

구조 전체는 `docs/01-project-map.md`, 담당 구분은 `docs/02-positions.md` 에 있습니다.

> **08 은 사람이 읽는 요약이고, 진행 상태의 정답은 `docs/07-next-tasks.md` 입니다.**
> 큰 덩어리(배포·데이터 모양 변경처럼 설명이 달라지는 것)를 끝냈으면 08 도 함께 갱신하세요.

---

## 사용자에게 말할 때

**사용자는 코드와 git 을 깊이 알지 않습니다.**

- 전문 용어를 쓸 때는 한 번 풀어서 설명하세요
- 사용자가 직접 명령어를 칠 필요가 없게, git 은 아래 규칙대로 **당신이 대신 실행**하세요
- 무엇을 왜 하는지 짧게 말하고 진행하세요. 매번 허락을 구하면 작업이 안 나갑니다
- 다만 **되돌리기 어려운 일**(파일 삭제, 브랜치 변경, 데이터 모양 변경)은 먼저 물어보세요

---

## 파일 구조 — 화면 하나에 파일 둘

> **1화면 = HTML 1개 + JS 1개.**

팀 시절에는 "주인도 1명" 이었습니다. 지금은 혼자라 주인 구분이 없어졌지만,
**파일을 나누는 방식은 그대로 두는 것이 좋습니다.** 화면 하나를 고칠 때
열어야 하는 파일이 둘로 정해져 있으면 어디를 봐야 할지 헷갈리지 않습니다.

| 트랙 | 파일 |
|---|---|
| 노동자 | `src/worker/home.* learn.* quiz.* report.* talk.* my.*` + `worker.css` |
| 관리자·운영자 | `src/admin/setup.* dashboard.* content.* proof.* library.*` + `admin.css` |
| 공용 | `src/assets/store.js auth.js ui.js seed.js diagrams.js review.js` + `app.css style*.css` |
| 검사 | `tests/harness.js run-all.js test-*.js` — 저장소 안, 의존성은 저장소 밖 |

`worker.css` 와 `admin.css` 안에는 `[P1]` `[P2]` 같은 구역 주석이 남아 있습니다.
팀 시절에 둘이 나눠 쓰던 표시입니다. 지우지 않아도 되고, 새 스타일은 아무 구역에 써도 됩니다.

### 공용 파일을 고칠 때

`src/assets/` 를 고치면 **여러 화면이 함께 영향을 받습니다.** 혼자여도 이건 그대로입니다.

- `store.js` 의 8개 키 **모양**을 바꾸면 그 키를 읽는 화면을 전부 확인합니다
  (어느 화면이 무엇을 읽는지는 아래 "데이터" 표에 있습니다)
- `ui.js` 의 기존 함수를 바꾸면 11화면이 다 영향을 받습니다.
  **더하는 것은 안전하고, 고치는 것은 확인이 필요합니다**
- 고친 뒤에는 회귀 검사를 돌립니다 (아래 "확인 방법")

### 화면 11개가 이미 동작합니다

10개가 껍데기였던 시절의 안내가 문서 곳곳에 남아 있을 수 있습니다. 지금은 아닙니다.

- 화면 11개 전부 실제 데이터로 동작합니다
- 그중 8개는 **골격** 입니다 — 데이터가 끝까지 흐르는 지점까지만 만들어져 있고,
  화면 맨 아래 **"여기부터 채우시면 됩니다"** 칸에 남은 일이 적혀 있습니다
- 각 파일 맨 위 주석에 **그 화면이 코드로 지키는 제품 원칙**이 적혀 있습니다.
  익명 신고 · 검수된 문구만 · 미이수 숨기지 않기 같은 것들입니다.
  **지워야 할 것 같으면 먼저 사용자에게 왜 그런지 물어보세요**


## 데이터 — 화면들이 만나는 지점

**`localStorage` 를 직접 부르지 마세요.** 반드시 `Store` 를 거칩니다.
나중에 서버가 생기면 `src/assets/store.js` 하나만 바꾸면 되기 때문입니다.

```js
var courses = Store.courses.load();            // 읽기
Store.progress.update(function (list) {        // 읽기 → 고치기 → 저장
  list.push({ workerId: user.userId, courseId: 'c-press', quiz: { passed: true } });
});
```

저장소마다 `load()` `save(data)` `update(fn)` `updatedAt()` `clear()` 가 있습니다.

| 저장소 | 쓰는 쪽 | 읽는 쪽 |
|---|---|---|
| `Store.accounts` | 회원가입 | 로그인 |
| `Store.session` | 로그인 | 모든 화면(가드) |
| `Store.setup` | 관리자 기능1 | 노동자 전부 · 관리자 전부 |
| `Store.library` | 운영자 기능9 | 관리자 기능2 · 노동자 기능3 |
| `Store.courses` | 관리자 기능2 | 노동자 기능3·4 |
| `Store.progress` | 노동자 기능3·4 | 관리자 기능5·6 |
| `Store.reports` | 노동자 기능8 | 관리자 기능6 |
| `Store.posts` | 노동자 기능7 | 노동자 기능7 |

**저장 데이터의 모양을 바꾸면 다른 사람 화면이 깨집니다.**
바꿔야 하면 사용자에게 "이건 다른 분 화면도 읽는 데이터라 팀 공유가 필요합니다" 라고 알리세요.

상대 팀 화면이 아직 없어도 `src/assets/seed.js` 의 예시 데이터로 개발할 수 있습니다.
로그인 화면의 **예시 데이터 채우기** 버튼이 8개 키를 전부 채웁니다.

---

## 코드 규칙

문서(`PRD-safety.pdf` `SCREEN-safety.pdf`)에서 온 제약입니다. 나중에 뜯어고치면 비용이 큽니다.

- **외부 요청 0건.** CDN · 웹폰트 · 외부 이미지 · npm 패키지를 쓰지 마세요.
  픽토그램은 이모지입니다. 덕분에 인터넷 없이 `file://` 로 열려 확인이 가장 빠릅니다
- **`innerHTML` 금지.** 사용자가 넣은 문자열은 `textContent` 로만 넣습니다 (`UI.el` 사용).
  설비 이름 한 줄로 화면이 깨지면 안 됩니다
- **ES 모듈(`import`/`export`) 금지.** `file://` 에서 CORS 로 막힙니다.
  기존 파일들처럼 `var X = (function () { ... })()` 형태로 쓰세요
- **색상만으로 상태를 구분하지 않습니다.** 배지는 아이콘 + 글자 + 색 3중
  (`UI.okBadge` `UI.waitBadge` `UI.stopBadge` `UI.neutralBadge`). 흑백으로 봐도 뜻이 남아야 합니다
- **터치 타깃** — 노동자 화면은 전부 60px(`--tap-min`). 장갑 낀 손으로 조작합니다.
  관리자 화면은 주요 버튼만 60px 이고 표 안 보조 조작은 40px(`.btn-sm`)
- **노동자 화면은 문해력을 전제하지 않습니다.** 모든 안내에 음성을 병행하고,
  글자를 한 자도 읽지 않고 끝낼 수 있어야 합니다
- **외국인등록번호 · 여권번호를 받지 않습니다.** 노동자는 사업장 내 식별번호만 씁니다
- **위험요소 신고는 익명입니다.** `Store.reports` 에 신고자를 식별할 값을 넣지 마세요.
  익명성이 깨지면 신고가 멈추고, 재해 감소의 선행지표도 함께 사라집니다
- **검수 완료가 아닌 문구는 안전 지시로 쓰지 않습니다.** `Store.library` 의 `status === 'reviewed'` 만.
  오역이 그대로 사고가 됩니다
- **"면책" 표현을 쓰지 않습니다.** 증빙은 교육 실시의 기록일 뿐 법적 책임을 대신하지 않습니다

### 새 화면을 만들 때

같은 트랙의 기존 화면을 복사해서 시작하세요. 상단바 · 하단 탭 · 화면 가드가 이미 맞춰져 있습니다.

경로는 **상대경로**로 씁니다 (`../assets/...`).
절대경로(`/assets/...`)를 쓰면 `file://` 로 열었을 때 깨집니다.

모든 페이지 `<head>` 에 이 세 줄이 순서대로 들어갑니다.

```html
<script src="../assets/store.js"></script>
<script src="../assets/auth.js"></script>
<script>Auth.require('worker');</script>
```

---

## git — 사용자 대신 당신이 합니다

**사용자가 명령어를 칠 필요가 없게 당신이 실행하세요.**

### 세션을 시작할 때

1. `git branch --show-current` — `feature/byeonsoyun` 인지 확인
2. `main` 이나 `develop` 에 있으면 **작업하지 말고** 사용자에게 알린 뒤 옮기세요
3. `git pull origin develop` — 다른 기기에서 작업했을 수 있으니 받아옵니다
4. 충돌이 나면 혼자 판단하지 말고 사용자에게 상황을 쉬운 말로 설명하세요

### 한 덩어리가 끝날 때마다

"한 덩어리"는 화면 하나가 동작하거나, 기능 하나가 붙은 시점입니다. 완벽하지 않아도 됩니다.

1. `docs/devlog/byeonsoyun.md` 에 오늘 한 일을 한 줄 추가 (아래 개발일지 규칙)
2. `docs/07-next-tasks.md` 에서 끝낸 항목을 `- [x]` 로 바꿉니다
3. `git add` — **이번에 고친 파일만.** `git add .` 대신 경로를 지정하세요.
   무엇을 고쳤는지가 커밋에 그대로 남아야 나중에 되짚을 수 있습니다
4. `git commit` — 커밋 메시지 규칙은 아래
5. `git push origin feature/byeonsoyun`

**하루에 여러 번 push 하는 것이 좋습니다.** 브라우저 저장소는 언제든 날아갑니다.

### 절대 하지 않는 것

- `main` 에 **직접 push 하지 마세요**
- 평소 작업은 `feature/byeonsoyun` 에 push 합니다.
  **덩어리가 끝나면 `develop` 에 머지**해 기준 브랜치를 최신으로 둡니다 (혼자라 PR 은 없습니다)
- **배포하고 싶을 때만 `develop` → `main` 을 앞으로 감기(`--ff-only`)로 넘깁니다.**
  `main` 에 올라가는 순간 배포 주소가 그것으로 바뀝니다.
  `main` 에서 **직접 작업하거나 작업 커밋을 올리는 것은 여전히 금지**입니다
- **★ push 순서는 `main` 이 먼저입니다.** `develop` 을 먼저 올리면 Vercel 이
  그 커밋을 이미 빌드한 것으로 보고 **`main` 배포를 건너뜁니다.**
  그러면 push 는 성공했는데 배포 주소만 옛날 것으로 남습니다 (2026-08-25 에 겪음).
  `git push origin main` → `git push origin develop` → `git push origin feature/byeonsoyun` 순서로 하세요
- **배포한 뒤에는 주소를 실제로 열어서 확인하세요.** push 가 됐다고 배포가 된 것이
  아닙니다. `curl -s <주소>/assets/ui.js | grep <이번에 넣은 함수 이름>` 이면 충분합니다
- `git push --force`, `git reset --hard` 는 사용자가 명시적으로 요청하지 않는 한 쓰지 마세요
- `--no-verify` 로 훅을 건너뛰지 마세요

### 커밋 메시지

`git-rules/GIT.md` 의 태그를 씁니다. 제목은 한글로, 무엇을 했는지 적습니다.

```
feat: 이해도 검증 문항 3유형 화면 구현
fix: 설비 삭제 시 노동자 소속이 남는 문제 수정
docs: 개발일지 갱신
style: 신고 화면 픽토그램 간격 조정
refactor: 문항 렌더링을 함수로 분리
chore: 예시 데이터에 도장 공정 추가
```

| 태그 | 언제 |
|---|---|
| `feat:` | 새 기능 추가 |
| `fix:` | 버그·오류 수정 |
| `docs:` | 문서 수정 |
| `style:` | 코드 포맷팅, 화면 간격 등 로직 변경 없음 |
| `refactor:` | 기능 변경 없이 구조 개선 |
| `chore:` | 설정·데이터 등 그 외 |

---

## 개발일지 — 한 덩어리가 끝날 때마다

며칠 뒤에는 "내가 뭘 만들었더라" 를 기억하지 못합니다.
**발표 준비할 때 이 기록이 대본의 재료가 됩니다.**
특히 "막혔던 것 / 어떻게 풀었는지" 가 발표에서 제일 설명하기 좋은 부분입니다.

commit 하기 직전에 `docs/devlog/byeonsoyun.md` 를 열고,
오늘 날짜 항목이 없으면 만들고 **한 것**에 한 줄 추가하세요.
**새 날짜는 맨 위에 쌓습니다.**

```markdown
## 2026-08-19

**한 것**
- 이해도 검증 문항 중 hotspot(설비 그림에서 위험 지점 짚기) 유형을 만들었다

**막혔던 것 / 어떻게 풀었는지**
- 터치 좌표가 화면 크기에 따라 달라져서, 퍼센트로 저장하도록 바꿨다

**다음에 할 것**
- choice(올바른 작업 고르기) 유형
```

- 짧게 쓰세요. 길면 안 쓰게 됩니다
- **막혔던 것을 빼먹지 마세요.** 이게 나중에 가장 쓸모 있습니다
- 사용자에게 "개발일지에 이렇게 적었습니다" 라고 한 줄 알려 주세요

---

## 확인 방법

빌드가 없으므로 서버를 띄울 필요가 없습니다.

1. `src/index.html` 을 브라우저로 엽니다 (더블클릭)
2. **예시 데이터 채우기** 를 누릅니다
3. 고친 화면의 계정으로 로그인합니다 — 비밀번호는 전부 `1234`

| 아이디 | 역할 | 도착 화면 |
|---|---|---|
| `W-4821-07` | 노동자 | `worker/home.html` |
| `kim@daesung.co.kr` | 담당자 | `admin/dashboard.html` |
| `oper@safety.kr` | 운영자 | `admin/library.html` |

4. 자기 화면으로 이동해 동작을 확인합니다
5. 개발자도구(F12) **Console** 에 빨간 오류가 없는지, **Network** 에 외부 요청이 없는지 봅니다

### 회귀 검사 — 브라우저 없이 돌리는 것

`tests/` 에 jsdom 검사가 있습니다. **실제 배포될 화면 스크립트를 그대로 돌립니다.**
동작만 보지 않고 **제품 원칙이 코드에 남아 있는지도** 봅니다 —
신고에 식별값이 없는지 · 미이수자를 숨기는 경로가 없는지 · 증빙에 "면책" 이 없는지.

`src/` 는 의존성 0 을 유지합니다. `jsdom` 은 **저장소 밖**에 깔고 `NODE_PATH` 로 가리킵니다.
**저장소 안에서 `npm install` 을 하지 마세요.**

```bash
mkdir -p ~/jsdom-box && cd ~/jsdom-box && npm i jsdom     # 한 번만
cd <저장소> && NODE_PATH=~/jsdom-box/node_modules node tests/run-all.js
```

- **`src/assets/` 의 공용 파일을 고쳤으면 반드시 돌립니다.** 11개 화면이 다 읽습니다
- 검사가 깨지면 먼저 **"내가 바꾼 것이 맞는 변화인가"** 를 봅니다.
  예시 데이터를 바꾸면 개수 기대값이 함께 깨집니다 — 그건 검사를 고치는 것이 맞습니다
- 자세한 것은 `tests/README.md`

화면을 고쳤으면 **사용자에게 무엇을 확인하면 되는지 구체적으로 알려 주세요.**
"확인해 보세요" 가 아니라 "신고 화면에서 프레스를 고르고 접수를 누르면 아래 목록에 뜹니다" 처럼.

---

## 저장 위치가 브라우저라서 생기는 일

이걸 모르면 고장으로 오해합니다. 사용자가 헷갈려 하면 설명해 주세요.

- **오리진별로 갈립니다.** `file://` · `localhost` · `*.vercel.app` 의 데이터가 전부 별개입니다.
  로컬에서 넣은 데이터가 배포 주소에 안 보이는 것은 정상입니다
- **다른 기기·다른 브라우저에서는 안 보입니다.** 발표 때 다른 사람 폰으로 열면 빈 화면입니다
- **시크릿 모드나 사이트 데이터 삭제 시 사라집니다.** 발표 전에는 백업하세요
- **약 5MB 제한.** 사진을 base64 로 넣으면 몇 장 만에 찹니다.
  사진이 필요해지는 시점이 서버가 필요해지는 시점입니다
