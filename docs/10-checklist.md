# 10. 내일 손으로 확인할 목록 — 성공/실패 케이스와 경계

**자동 검사 1,278건이 이미 통과했습니다.** 그런데도 이 목록이 필요한 이유는,
**검사가 못 보는 것**이 있기 때문입니다.

| 검사가 보는 것 | 검사가 **못 보는 것** |
|---|---|
| 데이터가 맞게 흐르는가 | **폰 카메라가 이 QR 을 읽는가** |
| 그 문장이 화면에 있는가 | **글을 못 읽는 사람이 실제로 끝까지 가는가** |
| CSS 규칙이 있는가 | **보기 좋은가 · 눌리는가** |
| 소리를 내라고 시켰는가 | **실제로 소리가 나는가 · 발음이 알아들을 만한가** |
| `sw.js` 에 규칙이 적혀 있는가 | **인터넷을 끊었을 때 진짜로 열리는가** |

> **여기 있는 것은 전부 "사람이 해야만 하는" 확인입니다.**

---

## 어떻게 읽나

각 항목은 이렇게 생겼습니다.

- **✅ 성공** — 이러면 맞습니다
- **❌ 실패** — 이러면 버그입니다. **어디를 봐야 하는지**까지 적었습니다
- **⬜ 경계(Acceptance boundary)** — **여기까지가 이 기능의 범위**입니다.
  이건 안 되는 게 **맞습니다.** 버그로 적지 마세요

---

## 0. 준비 (2분)

| | 할 것 |
|---|---|
| ☐ | `src/index.html` 을 **더블클릭** (또는 https://2026-ax-ton.vercel.app) |
| ☐ | 계정 목록에 **5개**가 이미 보인다 (자동으로 채워짐) |
| ☐ | F12 → **Console 빨간 오류 0건** · **Network 외부 요청 0건** |

> **파일로 열 때와 배포 주소로 열 때는 데이터가 별개입니다.** 정상입니다.
> QR 을 폰으로 찍는 확인만 **배포 주소**로 해야 합니다.

**계정** (비밀번호 전부 `1234`)

| 아이디 | 역할 | 상태 |
|---|---|---|
| `W-4821-07` | 노동자 · 크메르어 · 프레스 | c-press **통과** |
| `W-4821-11` | 노동자 · 인도네시아어 · 도장 | c-paint **미통과** + **재교육 지시 받음** |
| `W-4821-03` | 노동자 · 베트남어 · 프레스 | **미수강** |
| `kim@daesung.co.kr` | 담당자 | |
| `oper@safety.kr` | 운영자 | |

---

# ★★ A. 가장 중요한 두 가지

이 둘이 이 제품의 **핵심 주장**이고, 아직 **한 번도 제대로 확인 못 했습니다.**
다른 걸 다 못 해도 이 둘은 해 주세요.

## A1. QR 을 폰 카메라로 실제로 찍기

> **왜 중요한가** — 자동 검사는 "만든 QR 을 내가 다시 읽어서" 확인했습니다.
> 그건 **내 글씨를 내가 읽는 것**이라, 규격 자체를 오해했으면 못 잡습니다.
> 규격 상수(형식정보·버전정보·생성 다항식·용량표)를 대조해서 상당히 막아 뒀지만,
> **마지막 판정은 카메라입니다.**

**길** — PC 에서 https://2026-ax-ton.vercel.app → `kim@daesung.co.kr`
→ 하단 **콘텐츠** → 발급한 교육 표에서 **접속 주소** 버튼

- **✅ 성공**
  - QR 그림이 뜨고, 아래에 주소가 **글자로도** 보인다
  - 주소가 `https://2026-ax-ton.vercel.app/worker/learn.html?course=c-press` 꼴이다
    (상대경로 `../` 로 시작하면 **실패**)
  - **폰 카메라로 찍으면** 그 주소가 인식된다
  - 열면 → (로그인 안 돼 있으면) 로그인 화면 → 로그인 → **그 교육이 바로 열린다**
    (교육 목록이 아니라 **문구 첫 장**이 떠야 합니다)
- **❌ 실패와 볼 곳**

  | 증상 | 어디를 보나 |
  |---|---|
  | 카메라가 QR 을 **아예 못 알아봄** | `src/assets/qr.js` 의 격자 만들기. 화면을 키우거나 종이에 인쇄해서 다시 |
  | 주소는 읽히는데 **딴 글자** | `qr.js` 의 데이터 인코딩 / 블록 섞기 |
  | 열었는데 **로그인 화면에서 멈춤** | `assets/auth.js` 의 `loginUrl` · `assets/login.js` 의 `next` 처리 |
  | 로그인은 됐는데 **교육 목록**만 뜸 | `src/worker/learn.js` 맨 아래 `openFromQR()` |
  | 파일(`file://`)로 열었더니 주소가 `null/...` | 정상 아님 → `content.js` 의 `appBase()` |
- **⬜ 경계**
  - **`file://` 로 연 화면의 QR 은 배포 주소를 가리킵니다.** 그래서 찍으면
    "내 PC 에 넣은 데이터"가 아니라 배포 주소의 데이터가 보입니다 — **맞습니다.**
    화면에도 그렇게 적혀 있습니다
  - QR 은 **밝은 바탕에 어두운 칸**이어야 읽힙니다. 화면 밝기를 낮추거나
    어두운 곳에서 찍으면 안 읽힐 수 있습니다 — 코드 문제가 아닙니다
  - **주소가 213바이트를 넘으면 QR 을 안 만듭니다.** 대신 이유를 적습니다.
    지금 주소는 65자라 여유가 큽니다

## A2. 글자를 한 자도 안 읽고 끝까지 가기

> **왜 중요한가** — 이 제품의 존재 이유입니다.
> **만든 사람은 어디를 눌러야 할지 이미 알아서 스스로 확인이 어렵습니다.**
> 그래서 방법이 둘입니다.

**방법 1 — 혼자서**: 화면의 **한글을 일부러 안 읽고**, 그림·아이콘·소리만으로 갑니다.
**방법 2 — 더 정확함**: 다른 사람에게 **아무 설명 없이 폰을 건네고**,
"안전교육을 끝까지 해 보세요" 라고만 말합니다. **옆에서 아무 말도 하지 않습니다.**

**길** — 폰에서 `W-4821-03` (베트남어 · 미수강) 으로 로그인

- **✅ 성공**
  - 홈에서 **지금 무엇을 해야 하는지**를 그림·소리로 알 수 있다
  - 🎧 를 눌러 교육으로 가고, 문구를 끝까지 넘기고,
    👆 검증 3문항을 풀고, 결과 화면까지 **한글을 안 읽고** 도달한다
  - 막히는 자리가 있으면 **그 자리를 적어 둔다** ← 이게 이 확인의 결과물입니다
- **❌ 실패로 볼 것**
  - **글자를 읽어야만 다음으로 갈 수 있는 자리**가 하나라도 있으면 실패입니다
  - 소리가 안 나는데 **화면에 아무 말도 없으면** 실패입니다
    (안 나면 🔇 로 바뀌고 안내가 떠야 합니다)
- **⬜ 경계**
  - **베트남어 음성이 기기에 없을 수 있습니다.** 그때 한국어로 읽고
    **"내 언어 음성이 없습니다" 라고 화면에 적으면 맞습니다** — 조용히 넘어가면 실패
  - **재교육 지시 메모는 한국어입니다.** 못 읽는 게 맞고, 대신
    "담당자가 한국어로 남긴 말" 배지가 붙고 **할 일(🔁 · 교육 이름 · 다시 듣기 버튼)은
    글자 없이** 전달돼야 합니다
  - **번역이 없는 문구는 한국어 + "준비 중" 배지**로 나옵니다.
    조용히 숨기는 것보다 낫다고 정한 것입니다

---

# B. 오늘 만든 것 — 눈으로 확인

## B0. ★ 오프라인 — 비행기 모드로 잘라 보기 (E1)

> **왜 중요한가** — *"현장 인터넷이 약하다"* 가 이 제품의 전제이고,
> *"QR 을 설비 앞에 붙입니다"* 가 시연 문장입니다. 그 자리에 신호가 없으면
> 찍어도 아무것도 안 열립니다. **자동 검사는 sw.js 를 글로 읽었을 뿐이고,
> 실제로 담기는지는 브라우저만 압니다.**

**길** — 폰에서 https://2026-ax-ton.vercel.app 을 **한 번 엽니다** (여기서 담깁니다)
→ **비행기 모드 켜기** → 새로고침

- **✅ 성공**
  - 비행기 모드에서 새로고침해도 **로그인 화면이 그대로 뜬다**
  - 로그인해서 `W-4821-07` 홈으로 가면 메뉴 아래에 **"✓ 오프라인 가능"** 배지와
    *"…처음 한 번만 인터넷이 필요합니다"* 가 있다
  - 위쪽에 **📴 오프라인 — 이 기기에 저장된 화면으로 그대로 이어집니다** 띠가 뜬다
  - **QR 주소**(`.../worker/learn.html?course=c-press`)를 비행기 모드에서 열면
    **교육이 바로 열린다** ← `?course=` 가 붙어 있어도 열려야 합니다
  - 비행기 모드로 **이해도 검증을 끝까지 풀 수 있고** 기록이 남는다
  - 비행기 모드를 끄면 띠가 **저절로 사라진다**
- **❌ 실패와 볼 곳**

  | 증상 | 어디를 보나 |
  |---|---|
  | 비행기 모드에서 **아무 화면도 안 뜸** | 담기지 않았다. F12 → Application → Service Workers. `src/sw.js` 가 배포에 올라갔는지 (`curl -s <주소>/sw.js \| head`) |
  | 다른 화면은 열리는데 **한 화면만** 안 열림 | `src/sw.js` 의 `PRECACHE` 에서 그 파일이 빠졌다 |
  | **QR 주소만** 안 열림 (`learn.html` 은 열림) | `sw.js` 의 `ignoreSearch: true` |
  | 고쳐서 배포했는데 **옛 화면이 그대로** | `src/sw.js` 의 `VERSION` 을 안 올렸다. 탭을 완전히 닫았다 열기 |
  | **배지는 뜨는데 실제로는 안 열림** | 화면이 거짓말을 하고 있다 — `home.js` 의 `renderOfflineOk` / `UI.offlineReady` |
- **⬜ 경계**
  - **처음 한 번은 반드시 인터넷이 필요합니다.** 담을 것을 그때 받습니다.
    설비 앞에서 QR 을 *처음* 찍는 사람에게는 그 한 번이 있어야 합니다 — 맞습니다
  - **`file://` 로 열면 오프라인 배지가 안 뜹니다.** Service Worker 는 `https:`
    에서만 등록됩니다. 안 뜨는 것이 맞습니다
  - **오프라인은 이 기기 안에서만입니다.** 끊긴 동안 푼 검증 기록이 담당자에게
    저절로 가지 않습니다. 서버가 없어서 그렇고, 옮기려면 기능1 의 내보내기/불러오기입니다
  - **첫 방문 자동 채우기는 인터넷이 있을 때 일어납니다.** 비행기 모드로 처음 열면
    계정이 없는 것이 맞습니다

## B1. 재교육 지시 (D2)

**길** — `kim@daesung.co.kr` → **대시보드** → *조치가 필요한 사람*

- **✅ 성공**
  - `W-4821-11` 행에 **"재교육 지시"** 버튼이 있다
  - 누르면 표 **아래**에 입력칸이 열리고, 누구에게·무엇 때문인지가 적힌다
  - **한 줄도 안 적고** 보내면 안 보내지고 *"한 줄 적어 주세요"* 가 뜬다
  - 적고 보내면 → 표에 **"지시함 · 날짜"** 배지 + **"거두기"** 버튼
  - `W-4821-11` 로 로그인하면 **홈 맨 위**에 🔁 와 함께 뜬다
  - 그 카드에 **음성 버튼**과 **"다시 듣기"** 큰 버튼이 있다
  - **"담당자가 한국어로 남긴 말"** 배지가 메모 아래 붙어 있다
- **❌ 실패와 볼 곳**

  | 증상 | 어디 |
  |---|---|
  | 보냈는데 노동자 홈에 **안 뜸** | `Store.orderOpen` (`assets/store.js`) · `worker/home.js` 의 `myOpenOrders` |
  | **남의 지시가 보임** | `home.js` `myOpenOrders` 의 `workerId` 거르기 |
  | 거뒀는데 **기록까지 사라짐** | `dashboard.js` `cancelOrder` — 지우면 안 되고 `canceledAt` 만 |
- **⬜ 경계**
  - 지시를 보낸 **그 교육을 통과하면 저절로 사라집니다.** 담당자가 따로 닫지 않습니다
  - **"이 사람 재교육 n회" 같은 누적 횟수는 어디에도 없습니다.** 일부러 안 만들었습니다 —
    인사 평가 자료가 되기 때문입니다
  - **예전에 통과한 기록이 있어도** 지시는 사라지지 않습니다 (지시를 **내린 뒤**에
    통과해야 해소)

## B2. 관리자 공식 답변 (C7)

**길** — `kim@daesung.co.kr` → 대시보드 → **현장 소통 게시판 → 게시판 열기**

- **✅ 성공**
  - 게시판이 열리고 **"담당자로 보고 있습니다"** 안내가 뜬다
  - 안내 안에 **"← 담당자 화면으로 돌아가기"** 링크가 있다
  - 화면 아래 **노동자 탭바가 없다**
  - **글쓰기 버튼이 없다** (담당자는 답글만)
  - 첫 글을 열면 댓글 칸에 **이름 감추기 칩이 없고**, 왜 없는지 적혀 있다
  - 댓글을 달면 **"✓ 공식 답변"** 배지가 붙고 **김현수**라는 이름으로 보인다
  - `W-4821-07` 로 로그인해 같은 글을 보면 그 댓글만 **왼쪽 선 + 배지**로 구분된다
- **❌ 실패와 볼 곳**

  | 증상 | 어디 |
  |---|---|
  | 탭바를 누르면 **대시보드로 튕김** | `worker/talk.js` `applyRoleUI` — 탭바를 감춰야 함 |
  | 감춘 댓글에 **공식 답변 배지** | `talk.js` `isOfficial` — `anonymous` 면 무조건 false |
  | 담당자에게 **글쓰기 버튼이 보임** | `talk.js` `applyRoleUI` |
- **⬜ 경계**
  - **댓글은 수정·삭제 못 합니다** (글은 됩니다). 알고 있는 한계입니다
  - **운영자(`oper@safety.kr`)는 이 게시판에 못 들어옵니다.** 사업장 게시판이라
    그렇게 뒀습니다

## B3. 발급 후 문항 고치기 (C6)

**길** — `kim@daesung.co.kr` → **콘텐츠** → 발급한 교육 → **문항 고치기**

- **✅ 성공**
  - 3단계 카드가 **그 교육의 문항**으로 바뀌고, 위에 **"고치는 중"** 안내가 뜬다
  - 1·2·4단계가 **감춰진다**
  - 프레스 교육(누가 이미 풀었음)의 문항에는 **삭제 버튼이 없고**,
    *"이미 이 교육을 푼 사람이 있어…"* 라는 **이유가 적혀 있다**
  - **"내리기"** 를 누르면 배지가 **"내려짐"** 으로 바뀐다
  - 새 문항을 추가하면 **맨 뒤**에 붙는다
  - **"고치기 끝내기"** 를 누르면 원래 화면으로 돌아오고, **초안이 오염되지 않았다**
  - `W-4821-07` 로 검증에 들어가면 **내린 문항이 안 나온다** (3문항 → 2문항)
  - 그래도 담당자 **대시보드의 취약 항목이 안 깨진다** ← 이게 이 항목의 핵심
- **❌ 실패와 볼 곳**

  | 증상 | 어디 |
  |---|---|
  | 문항을 내렸더니 대시보드 항목 이름이 **엉뚱하게 바뀜** | `Store.askedQuestion` · `progress.quiz.asked` |
  | 내렸는데 노동자에게 **계속 나옴** | `worker/quiz.js` `questionsOf` |
  | 문항 추가했더니 **앞 문항 번호가 바뀜** | `content.js` `pushQuestion` — 맨 뒤에만 |
- **⬜ 경계**
  - **문항 문구 자체는 못 고칩니다.** 고치면 번역이 그대로 남아 한국어와 다른 것을
    묻게 됩니다. **내리고 새로 붙이는 것**이 맞는 길입니다
  - **선택지 순서·정답 번호도 못 바꿉니다.** 옛 기록이 다른 선택지를 가리킵니다
  - **내린 문항도 증빙에는 그대로 남습니다** ("지금은 내려진 문항입니다" 표시).
    그 문항으로 이해를 확인한 사람이 실제로 있었기 때문입니다

## B4. PC 레이아웃 (D3)

**길** — PC 브라우저에서 `kim@daesung.co.kr` → 대시보드 → **창을 넓게**

- **✅ 성공**
  - 창이 **1024px 이상**이면 하단 탭바가 **왼쪽 세로 메뉴**가 된다
  - 카드가 **두 칸**으로 간다
  - **★ 이해도 취약 항목(정답률 막대)은 여전히 한 줄 전체**를 쓴다 —
    이수 현황 타일보다 **커야** 합니다
  - 창을 좁히면 **예전 모양 그대로** 돌아온다
  - 노동자 화면(`W-4821-07`)은 넓은 화면에서 **폰 너비로 가운데** 모인다
- **❌ 실패와 볼 곳**

  | 증상 | 어디 |
  |---|---|
  | 취약 항목이 **반으로 줄어듦** | `admin/admin.css` 의 `.card.feature { grid-column: 1 / -1 }` |
  | 세로 메뉴가 **상단 머리띠를 덮음** | `admin.css` 의 `.apphead { z-index: 40 }` · 세로 메뉴 `padding-top: 76px` |
  | 노동자 화면이 **가로로 늘어남** | `worker/worker.css` 의 `@media (min-width: 900px)` |
- **⬜ 경계**
  - **1024px 미만은 한 픽셀도 안 바뀝니다.** 담당자도 현장에서는 폰으로 봅니다
  - 노동자 화면은 **두 벌로 만들지 않았습니다.** 폰이 본체이고, 큰 버튼·큰 글자가
    가로로 늘어나면 오히려 읽기 어렵습니다
  - 세로 메뉴 높이가 화면보다 길어지면 잘릴 수 있습니다 (메뉴 5개라 지금은 여유)

## B5. 목업에서 옮겨 온 문장 (B4)

**길** — `W-4821-07` 홈

- **✅ 성공**
  - "내 교육" 카드에 **"이해도 검증을 통과해야 교육이 완료됩니다.
    수강만으로는 완료로 기록되지 않습니다."** 가 있다 ← **이 제품이 서 있는 자리**
  - 오늘의 안전 문구에 **"✓ 검수 완료"** 배지
  - 메뉴 4칸에 배지 — *n개 중 m개 남음* · **익명으로 접수** · 공식 답변 표시 · 증빙 출력
  - 메뉴의 🔊 를 누르면 **배지 내용까지 읽어 준다** (아이콘은 안 읽음)
  - 대시보드에 *"생성된 기록은 수정할 수 없고, … 숨기는 경로는 없습니다"*
  - 라이브러리에 *"검수 완료가 아닌 문구는 어떤 화면에서도 안전 지시로 노출되지 않습니다"*
- **⬜ 경계**
  - 목업의 **"오프라인 가능"** 배지는 **일부러 안 옮겼습니다.** 지금 오프라인으로
    안 돕니다. 화면이 거짓말을 하면 안 됩니다
  - 목업의 **"사진 보기"**, **"조치 결과를 신고자 언어로 회신"** 도 같은 이유로 뺐습니다

---

# C. 제품 원칙 — 어겨졌는지 확인

> 자동 검사가 지키고 있지만, **눈으로 한 번 보는 것**이 다릅니다.
> 여기 하나라도 어겨져 있으면 **버그가 아니라 사고**입니다.

| | 확인할 것 | 어디서 |
|---|---|---|
| ☐ | 신고를 접수한 뒤 **누가 신고했는지 알 수 있는 화면이 하나도 없다** | 기능8 접수 → 기능6 신고 큐 · 마이페이지 |
| ☐ | 증빙에 **미이수자가 그대로 나온다.** 걸러 보기·정렬이 없다 | `kim@` → 증빙 |
| ☐ | 증빙 어디에도 **"면책"** 이라는 낱말이 없다 | 증빙 화면 전체 |
| ☐ | 검증에서 떨어졌을 때 **"노동자의 실패가 아니라 교육의 실패"** 가 뜬다 | `W-4821-11` → 검증 |
| ☐ | 대시보드에 **세 문장**이 다 있다 (콘텐츠 개선 신호 · 교육의 실패 · 인사 목적 내보내기 없음) | 대시보드 |
| ☐ | **검수 대기 문구가 교육 만들기 선택지에 아예 안 나온다** | `kim@` → 콘텐츠 2단계 |
| ☐ | `ph-3` 이 **인도네시아어만 중지**돼 있다 | `oper@` → 라이브러리 |
| ☐ | 그래서 `W-4821-07`(크메르어)는 그 문구를 **계속 듣고**, `W-4821-11`(인니어)는 **안 본다** | 두 계정으로 수강 화면 |
| ☐ | 모든 배지가 **아이콘 + 글자 + 색** 3중이다 (흑백으로 봐도 뜻이 남는다) | 아무 화면 |
| ☐ | 노동자 화면 버튼이 **장갑 낀 손으로 눌릴 만큼 크다** | 폰에서 |

---

# D. 망가뜨려 보기 — 이렇게 해도 안 깨져야 합니다

> 자동 검사가 하는 일과 같은 것을 **손으로** 해 보는 것입니다.
> 시연 중에 사고가 나는 자리이기도 합니다.

| | 무엇을 해 보나 | 이러면 ✅ |
|---|---|---|
| ☐ | 설비 이름에 **아주 긴 글자**를 넣고 저장 | 화면이 안 깨지고 줄바꿈된다 |
| ☐ | 게시글 제목에 `<script>alert(1)</script>` | **글자 그대로** 보인다 (실행 안 됨) |
| ☐ | 문항을 **하나도 안 만들고** 발급 시도 | 막힌다 (*"문항 없이는 발급 불가"*) |
| ☐ | 검증에서 **같은 문항을 두 번** 누르기 | 두 번째는 무시된다 |
| ☐ | **시크릿 모드**로 열기 | *"이 브라우저에서는 저장소를 쓸 수 없습니다"* 가 뜬다 |
| ☐ | 로그인 안 한 채로 `worker/quiz.html` 주소 직접 입력 | 로그인으로 가고, 로그인하면 **그 화면으로 이어진다** |
| ☐ | `W-4821-07` 로 로그인한 채 `admin/dashboard.html` 입력 | **노동자 홈**으로 돌아간다 (튕기지 않고) |
| ☐ | **전체 초기화** 후 새로고침 | 예시 데이터가 다시 채워진다 |
| ☐ | 글자 크기를 **크게** 한 뒤 예시 데이터 채우기 | 글자 크기가 **안 되돌아간다** |
| ☐ | 카카오톡으로 링크를 보내 **카톡 안에서** 열고 🔊 누르기 | 🔇 로 바뀌고 *"소리가 나지 않습니다 … 다른 브라우저로 열기"* |

---

# E. 시연 리허설 (5분 코스)

**발표에서 보여 줄 순서입니다.** 한 번 돌려 보고 막히는 자리를 적어 두세요.

| 순서 | 무엇 | 하고 싶은 말 |
|---|---|---|
| 1 | `oper@safety.kr` → 라이브러리 → `ph-3` | *"인도네시아어 번역만 뜻이 뒤집혔습니다. **그 언어만** 내렸습니다"* |
| 2 | `W-4821-07`(크메르어) → 수강 | *"크메르어 노동자는 이 안전 지시를 **계속 듣습니다**"* |
| 3 | 같은 계정 → 이해도 검증 👆 | *"글로 답하지 않습니다. 설비 그림에서 위험한 곳을 짚습니다"* |
| 4 | 일부러 틀려 보기 | *"맞고 틀림만 알려 주면 다음에 또 같은 선택을 합니다. **어떤 사고가 되는지**를 말합니다"* |
| 5 | `kim@` → 대시보드 | *"이수율이 아니라 **어느 언어가 무엇을 못 알아들었는지**를 봅니다"* |
| 6 | 조치 대상 → **재교육 지시** | *"이건 '이 교육을 다시 듣게 한다'는 뜻이지 '이 사람이 못했다'가 아닙니다"* |
| 7 | 콘텐츠 → **접속 주소** → QR | *"설비 앞에 붙입니다. 찍으면 그 설비 교육이 바로 열립니다"* |
| 8 | 증빙 → 인쇄 미리보기 | *"서명이 아니라 **이해도 검증 결과**입니다. 미이수자를 숨길 경로가 없습니다"* |
| 9 | **폰을 비행기 모드로 바꾸고 새로고침** | *"공장 안에는 신호가 없습니다. **끊어도 그대로 열립니다.** QR 을 설비 앞에 붙이는 것이 그래서 말이 됩니다"* ← 시연에서 가장 눈에 띄는 한 수 |

**시연 전 준비**

- ☐ 배포 주소를 미리 한 번 열어 둔다 (첫 방문 자동 채우기가 돌게)
- ☐ 폰과 PC **둘 다** 데이터를 채워 둔다 (기기마다 별개입니다)
- ☐ **시크릿 모드로 시연하지 않는다** (껐다 켜면 사라집니다)
- ☐ 발표 직전 **기능1 → 내보내기**로 JSON 백업

---

# F. 확인 결과 적는 칸

발견한 것을 여기 적어 두면, 다음 세션에서 Claude Code 가 그대로 이어서 고칩니다.

```markdown
## 2026-08-29 확인 결과

### 잘 된 것
-

### 막힌 것 / 이상한 것
- (어디서) (무엇을 했더니) (무엇이 일어났는지)
-

### 다른 사람에게 폰을 건네 본 결과
- 막힌 자리:
- 물어본 것:
```

---

## 함께 볼 것

| 무엇 | 어디 |
|---|---|
| 시스템 전체 인계 | [`09-handover.md`](09-handover.md) |
| 진행 상태의 정답 | [`07-next-tasks.md`](07-next-tasks.md) |
| 자체 평가 · 차별점 | [`11-evaluation.md`](11-evaluation.md) |
| 뭔가 안 될 때 | [`05-troubleshooting.md`](05-troubleshooting.md) |
