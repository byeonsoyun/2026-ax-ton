# 03. 검증 신뢰도와 숫자 — 실제로 되는지 어떻게 확인했나

> **★★ 먼저 이것부터. 기존 발표 문서의 숫자를 쓰면 안 됩니다.**
>
> `docs/presentation/` 과 `docs/11-evaluation.md` 는 작성 시점이 달라
> **서로 다른 숫자를 말하고 있습니다.** 심사위원이 두 슬라이드에서 다른 숫자를 보면
> 그것 하나로 나머지 전부를 의심합니다. **아래 §1 의 숫자만 쓰세요.**

---

## 1. ★ 외울 숫자 한 장 (2026-08-30 실측)

### 만든 것

| 무엇 | 숫자 | 어떻게 셌나 |
|---|---:|---|
| 화면 | **11개** (+ 로그인·회원가입) | `src/worker/` 6 + `src/admin/` 5 |
| 자동 검사 | **1,558건 / 묶음 15개** | `node tests/run-all.js` |
| 제품 코드 | **16,713줄** (파일 41개) | `src/` 의 html·js·css |
| 검사 코드 | **6,606줄** | `tests/` |
| 커밋 | **86개** | `git rev-list --count HEAD` |
| 개발일지 | **27편** | `docs/devlog/byeonsoyun.md` |
| 외부 요청 | **0건** | 예외 2개는 화면에 적어 뒀음 (아래 §4) |
| 지원 언어 | **6개** (한국어 + 5) | 크메르·인도네시아·베트남·네팔·타이 |
| 안전 문구 | **6개** (예시) · 목표 200개 | `Store.library` |
| 문항 유형 | **3유형** 전부 비문자 조작 | hotspot · choice · match |
| 데이터 칸 | **10칸** | `Store` |
| 터치 타깃 | **60px** 이상 | 장갑 낀 손 |
| 개발 기간 | **10일** | 2026-08-19 ~ 08-30 |

### 문제의 크기 (출처는 `../11-evaluation.md` 16건)

| 무엇 | 숫자 |
|---|---:|
| 외국인 산재 피해자 | **2022년 8,286명 → 2024년 9,219명** |
| 한 해 사망 | **100명 넘게** |
| 국내 외국인 노동자 | **약 92만 명** |
| 공공 다국어 교육자료 | **16~17개 언어** |
| 안전보건 픽토그램 | **20종** |

---

## 2. ★ 정정표 — 옛 숫자를 봤다면 이렇게 바뀌었습니다

| 무엇 | `presentation/` 의 값 | `11-evaluation.md` · `08-overview.md` | **지금 실제** |
|---|---:|---:|---:|
| 자동 검사 | 1,278건 | 1,219건 | **1,558건** |
| 검사 묶음 | 11개 | 10개 | **15개** |
| 개발일지 | 23편 | 21편 | **27편** |
| 커밋 | 81개 | — | **86개** |
| 제품 코드 | 15,301줄 | 약 15,000줄 | **16,713줄** |
| 검사 코드 | 5,576줄 | 약 5,400줄 | **6,606줄** |
| 출처 | "15건" | 실제 목록은 16줄 | **16건** |

> **왜 늘었나** — 발표 문서를 쓴 뒤에 세 덩어리를 더 만들었습니다.
> **화면 다국어(UI-1)** · **음성 신고** · **음성 되돌림**.
> 세 개 다 발표에서 쓸 수 있는 이야기입니다 (§3, [`06`](06-한계와-개선-로드맵.md) 참고).

---

## 3. 검사 14묶음이 각각 무엇을 보나

| 묶음 | 단정 | 무엇을 지키나 |
|---|---:|---|
| 노동자 화면 4개 | **262** | 홈·신고·소통·마이가 실제로 도는가 |
| 기능2 콘텐츠 생성 | **152** | 검수된 문구만 선택지에 · 문항 없이 발급 불가 |
| 기능9 문구 라이브러리 | **136** | 언어별 판정 · 역번역 대조 · 검수 게이트 |
| 기능6 대시보드 | **122** | **미이수를 숨기는 경로가 없는가** · 기간 기준이 발급일인가 |
| 기능4 이해도 검증 | **119** | 3유형 채점 · 한 문항 한 번 · 사고 결과 설명 |
| 회귀(smoke) | **115** | 화면 11개 부팅 · 데이터 계약 · **코드 규칙** · 음성 실패 감지 |
| 기능5 증빙 | **102** | **저장하는 코드가 없는가** · "면책" 없음 · 미통과 숨김 없음 |
| **음성 되돌림** | **98** | **뜻이 안 닿는 소리를 내지 않는가** (2026-08-30 추가) |
| **선 아이콘** | **95** | **화면 13개를 그려 보고 이모지가 남았는지** (2026-08-30 추가) |
| 문서 | **96** | 끝난 기능이 아직 "할 일" 로 적혀 있지 않은가 |
| QR 인코더 | **75** | 외부 라이브러리 없이 만든 QR 이 규격에 맞는가 |
| 오프라인(E1) | **54** | `sw.js` 목록에 새 화면이 빠지지 않았는가 |
| 기능3 수강 | **52** | 번역 없는 문구를 조용히 숨기지 않는가 |
| 화면 다국어(UI-1) | **51** | **사전에 안전 지시가 섞이지 않았는가** |
| 음성 신고 | **29** | **목소리가 저장되지 않는가** (익명성) |

> ### 발표에서 쓸 한 수
> *"검사가 **동작만** 보지 않습니다. **제품 원칙이 코드에 남아 있는지**도 봅니다.
> 신고에 신고자를 식별할 값을 넣으면 검사가 깨집니다.
> 증빙에 '면책' 이라는 단어를 쓰면 검사가 깨집니다.
> 사전에 '손을 넣지 마십시오' 같은 문장을 넣으면 검사가 깨집니다 —
> 그건 검수를 우회하는 통로가 되기 때문입니다."*

---

## 4. 검증 4겹 — 어떻게 확인했나

### 겹 1. 자동 검사 (기계)

`tests/` 는 **실제 배포되는 화면 스크립트를 그대로** 돌립니다. 흉내 낸 코드가 아닙니다.
jsdom 으로 진짜 DOM 을 만들고, `store.js` → `auth.js` → `i18n.js` → 화면 JS 를
**실제 페이지와 같은 순서로** 실행합니다.

### 겹 2. ★ 일부러 망가뜨려 보기 (이게 이 프로젝트의 방식입니다)

**통과 개수가 늘어난 것으로 만족하지 않습니다.** 검사를 믿지 않으려고 코드를 일부러 깨 봅니다.

가장 최근 예 — **음성 되돌림에서 10가지를 망가뜨렸고 10가지 전부 잡혔습니다.**

| 무엇을 망가뜨렸나 | 잡혔나 |
|---|---|
| 소리 안 냄인데 한국어로 읽어 버리게 | 잡음 |
| 기본값을 한국어로 되돌리게 | 잡음 |
| 이상한 값 검증을 빼기 | 잡음 |
| 버튼 그림이 안 바뀌게 | 잡음 |
| 화면이 다시 그려 달라고 안 알리게 | 잡음 |
| 되돌림 표와 사전을 어긋내기 (양방향) | 잡음 |
| 한국어도 되돌림 대상으로 만들기 | 잡음 |
| 번역 배지 가드 빼기 | 잡음 |
| 선택 칩 하나 빼기 | 잡음 |

> **말할 것** — *"검사가 안 잡으면 그 검사는 아무것도 지키지 않는 겁니다.
> 그래서 **잡히는지 확인하고 나서야** 그 검사를 믿습니다."*

### 겹 3. 손으로 확인 (사람)

- **안드로이드 폰 실기** — 버튼이 장갑 낀 손 크기로 눌리는지, 크메르어 글자가 안 깨지는지
- 여기서 **카카오톡 안에서 음성이 조용히 안 나는 것**을 찾았습니다 → 고쳤습니다
- **비행기 모드로 잘라 보기** — 인터넷 없이 화면이 열리는지
- 순서는 [`../10-checklist.md`](../10-checklist.md) 에 있습니다

### 겹 4. 배포된 주소를 실제로 열어 보기

**push 됐다고 배포된 것이 아닙니다.** 매번 실제 주소를 열어 확인합니다.

```
curl -s https://2026-ax-ton.vercel.app/assets/ui.js | grep notifyVoice
```

그리고 `/docs/` `/tests/` `/CLAUDE.md` 가 **404 인 것**도 확인합니다 —
문서와 검사 코드가 공개되면 안 되니까요.

---

## 5. ★★ 검사가 증명하는 것 / 증명하지 못하는 것

**이 절이 신뢰도의 핵심입니다.** 1,463건을 자랑하는 것보다 **한계를 아는 것**이 점수가 됩니다.

| 검사가 **증명하는 것** | 검사가 **증명하지 못하는 것** |
|---|---|
| 화면이 실제 데이터로 끝까지 돈다 | **사람이 그 화면을 이해하는지** |
| 신고에 식별값이 저장되지 않는다 | 현장에서 **실제로 신고를 하는지** |
| 미이수를 숨기는 경로가 코드에 없다 | 감독기관이 이 증빙을 **인정하는지** |
| 검수 안 된 문구가 화면에 안 나온다 | **번역이 실제로 맞는지** (역번역도 같은 사람이 넣음) |
| 소리가 났는지 안 났는지 판정한다 | **발음이 알아들을 만한지** |
| 외부 요청이 0건이다 | 서버가 생겼을 때도 그럴지 |
| QR 이 규격에 맞게 인코딩된다 | **폰 카메라가 실제로 읽는지** (아직 안 찍어 봤습니다) |

> ### 발표에서 쓸 한 수
> *"검사 1,463건이 증명하는 것은 **'코드가 우리가 정한 원칙을 어기지 않았다'** 까지입니다.
> **'사람이 이해했다' 는 코드로 증명할 수 없습니다.**
> 그건 실제 노동자에게 보여 줘야 알 수 있고, 저희는 아직 못 했습니다."*

**이렇게 말하는 것이 "1,463건 통과했습니다" 보다 강합니다.**

---

## 6. 외부 요청 0건 — 예외 2개까지 말하는 것이 정확합니다

**"외부 요청 0건" 은 이 프로젝트의 자랑입니다.** CDN · 웹폰트 · 외부 이미지 · npm 패키지가
하나도 없습니다. **픽토그램은 좌표만 든 선 아이콘**이고 (`assets/icons.js`),
**QR 도 직접 만들었습니다** (`assets/qr.js`). 아이콘 폰트조차 쓰지 않습니다.

그런데 **예외가 정확히 2개** 있고, 숨기지 않았습니다.

| 예외 | 무엇 | 어디에 적어 뒀나 |
|---|---|---|
| `sw.js` 의 `fetch(req)` | 같은 오리진 요청을 그대로 흘려보냄 | 코드 주석 + 검사가 "외부 주소 0건" 을 따로 지킴 |
| 신고 화면의 **받아쓰기** | 브라우저가 소리를 인식 서버로 보냄 | **화면에 적혀 있음** — *"이 기능만 인터넷이 필요합니다"* |

> **말할 것** — *"받아쓰기는 저희가 서버를 부르는 게 아니라 브라우저가 부르는 겁니다.
> 그래도 결과는 같으니 **화면에 적었습니다.** 예외를 셋으로 늘리지 않는다는 규칙도
> `CLAUDE.md` 에 넣었습니다."*

---

## 함께 볼 것

- [`02-아키텍처와-플로우차트.md`](02-아키텍처와-플로우차트.md) 그림 7 — 검증 파이프라인
- [`05-PPT-프로그램-대조표.md`](05-PPT-프로그램-대조표.md) — 말해도 되는 것 / 안 되는 것
- [`06-한계와-개선-로드맵.md`](06-한계와-개선-로드맵.md) — §5 의 한계를 어떻게 메울지
- [`../presentation/05-정말-동작하는가.md`](../presentation/05-정말-동작하는가.md) — 원본 (숫자는 이 문서가 최신)
